server {
  listen 80;
  # listen 443 ssl; ssl_certificate ...;

  location /sales/ {
    proxy_pass http://sales:8000/mcp;
  }
  location /ops/ {
    proxy_pass http://ops:8040/mcp;
  }
  location /performance/ {
    proxy_pass http://performance:8020/mcp;
  }
}
